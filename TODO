1.  Talk about using <chrono> shows that run time per gen doesn't hit
   an equilibrium until about 10N generations.
2. Check if moving adjust_mutation_counts call is a good idea in the
   multilocus sampler